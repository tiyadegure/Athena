# Athena — Workflow v5.1：全面清理 + 统一命名

> v5.1：清理所有遗留文件，统一项目名称为 Athena，补齐缺失内容。
>
> 目标：一次性修复所有过时内容，不做增量补丁。

---

## 1. 问题清单

### 1.1 命名问题（GLM Audit → Athena）

| 文件 | 问题 | 修改 |
|------|------|------|
| `README.md` | 已改标题，但正文多处仍提 "GLM-5.1" 作为项目名 | 改为 "Athena"，GLM-5.1 只在技术描述中出现 |
| `PLAN.md` | 标题 "GLM Audit"，全文 10+ 处旧名 | 全文改 Athena |
| `TESTING-PLAN.md` | 标题 "GLM Audit" | 改 Athena |
| `frontend/index.html` | title/Logo/footer "GLM AUDIT" | 改 "ATHENA" |
| `frontend/style.css` | 注释 "GLM AUDIT" | 改 "ATHENA" |
| `frontend/app.js` | 注释 "GLM AUDIT" | 改 "ATHENA" |
| `demo/nft-preview.html` | 标题 "Audit Certificate NFT"，SVG 底部 "GLM AUDIT" | 改 "ATHENA" |
| `mcp/tools/README.md` | 标题 "GLM Audit Agent" | 改 "Athena" |
| `mcp/servers.json` | description 里 "GLM Audit Agent" | 改 "Athena" |
| `prompts/audit-system.md` | 标题 "GLM Audit Agent" | 改 "Athena" |
| `prompts/system.md` | 标题 "GLM Audit Agent" | 改 "Athena" |
| `skills/glm-audit-skill/SKILL.md` | name/description 里 "GLM" | 改 "athena-audit-skill" |
| `contracts/AuditCertificate.sol` | SVG 底部文字 "GLM AUDIT" | 改 "ATHENA" |
| `contracts/AuditEvidenceChain.sol` | 检查是否有 GLM 引用 | 改 |
| `docs/ZK-INSPIRATION.md` | 检查是否有 GLM 引用 | 改 |

### 1.2 内容过时问题

| 文件 | 问题 | 修改 |
|------|------|------|
| `README.md` | 项目结构只列 6 个目录，缺少 options-index/real-world/frontend/workflows/docs | 更新完整目录树 |
| `README.md` | 路线图还是 Phase 1-4 旧版 | 更新为 v5 Demo 流程 |
| `README.md` | 技术栈只列 6 个 MCP 工具 | 更新为 12 个 |
| `README.md` | NFT 描述还是"像素风" | 更新为 generative trait 12,000 种组合 |
| `PLAN.md` | 架构图只列 6 个 MCP 工具 | 更新为 12 个 |
| `PLAN.md` | 项目结构还是旧版目录树 | 更新 |
| `PLAN.md` | 执行计划还是 Phase 1-4 | 更新为 v5 Demo 流程 |
| `PLAN.md` | 测试集只有 5 个合约 | 更新为 17 个 |
| `mcp/tools/README.md` | 只列 6 个工具 | 更新为 12 个完整列表 |
| `mcp/servers.json` | 只注册 6 个服务器 | 补齐 7 个新工具 |
| `prompts/audit-system.md` | 只列 6 个 MCP 工具 | 更新为 12 个 + v5 Demo 流程 |
| `prompts/system.md` | 只列 6 个 MCP 工具 | 更新为 12 个 + v5 Demo 流程 |
| `contracts/test-cases/expected-results.json` | 只有 5 个测试合约 | 补齐 17 个 |
| `requirements.txt` | 缺少新工具依赖 | 补齐 |
| `demo/nft-preview.html` | 旧版 3 个静态 SVG | 更新为 generative 版本（至少展示 3 个不同 trait 组合） |

### 1.3 不需要改的文件

| 文件 | 原因 |
|------|------|
| `workflows/v1-v5.md` | 历史记录，保持原样 |
| `contracts/*.sol` | 合约代码，除了 SVG 文字外不需要改 |
| `mcp/tools/*.py` | 工具代码，不需要改 |
| `contracts/real-world/*` | 测试数据，不需要改 |
| `contracts/options-index/*` | 测试数据，不需要改 |
| `contracts/test-cases/*` | 测试数据，不需要改（expected-results 除外） |
| `frontend/style.css` | 只改注释，样式不变 |
| `frontend/app.js` | 只改注释，逻辑不变 |
| `.gitignore` | 不需要改 |
| `foundry.toml` | 不需要改 |
| `remappings.txt` | 不需要改 |
| `GLM-5.1-TECHNICAL-PROFILE.md` | 技术文档，保持原样 |
| `smart-contract-audit-agents-comparison.md` | 调研报告，保持原样 |
| `script/DeployCertificate.s.sol` | 部署脚本，不需要改 |
| `contracts/test/AuditCertificate.t.sol` | 测试，不需要改 |

---

## 2. 执行步骤

### Step 1：批量重命名（GLM → Athena）

所有文件中的 "GLM Audit Agent" → "Athena"
所有文件中的 "GLM AUDIT" → "ATHENA"
所有文件中的 "GLM Audit" → "Athena"
所有文件中的 "glm-audit" → "athena"（文件名/目录名除外）
所有文件中的 "glm_audit" → "athena"

**注意**：
- `GLM-5.1` 作为模型名保持不变（它是 Z.AI 的模型名）
- `glm-audit-skill` 目录名保持不变（避免破坏引用），只改 SKILL.md 内部
- `GLM-5.1-TECHNICAL-PROFILE.md` 文件名保持不变

### Step 2：更新 README.md

重写以下部分：
- 项目结构：完整目录树（包含所有 v3/v4/v5 新增）
- 技术栈：12 个 MCP 工具完整列表
- NFT 描述：generative trait 12,000 种组合
- 路线图：v5 Demo 流程（8 步闭环）
- 竞品对比：更新核心优势描述

### Step 3：更新 PLAN.md

重写以下部分：
- 标题和定位
- 架构图：12 个 MCP 工具
- 项目结构：完整目录树
- 执行计划：v5 Demo 流程
- 测试集：17 个测试合约
- 差异化叙事：v5 长程任务完整闭环

### Step 4：更新 mcp/tools/README.md

完整列出 12 个工具：

| 工具 | 脚本 | 功能 |
|------|------|------|
| Slither | slither_runner.py | 静态分析 |
| Aderyn | aderyn_runner.py | Rust 静态分析 |
| PoC Generator | poc_generator.py | PoC 生成 |
| Fuzz Runner | fuzz_runner.py | Foundry fuzz |
| Knowledge Base | knowledge_base.py | RAG 知识库 |
| EAS Attestation | eas_attest.py | 链上认证 |
| Exploit Simulator | exploit_simulator.py | 攻击模拟器 |
| Evidence Chain | evidence_chain.py | 审计证据链 |
| Halmos | halmos_runner.py | 形式化验证 |
| Protocol Scanner | protocol_scanner.py | 协议级扫描 |
| Repair Validator | repair_validator.py | 修复验证 |
| Incremental Auditor | incremental_auditor.py | 增量审计 |
| GEV Analyzer | gev_analyzer.py | GEV 分析 |

### Step 5：更新 mcp/servers.json

补齐 7 个新工具的注册：
- exploit_simulator
- evidence_chain
- halmos_runner
- protocol_scanner
- repair_validator
- incremental_auditor
- gev_analyzer

### Step 6：更新 prompts/audit-system.md

- 标题改 Athena
- MCP 工具列表更新为 12 个
- 审计流程更新为 v5 Demo 8 步闭环

### Step 7：更新 prompts/system.md

- 标题改 Athena
- MCP 工具列表更新为 12 个
- 工作流程更新为 v5 Demo 8 步闭环

### Step 8：更新 contracts/test-cases/expected-results.json

补齐新测试合约的期望结果：
- AgentEscrow.sol
- AgentIdentity.sol
- GEVTest.sol
- PrivacyToken.sol
- ReadOnlyReentrancy.sol

### Step 9：更新 demo/nft-preview.html

- 标题改 ATHENA
- SVG 底部文字改 ATHENA
- 展示 3 个不同 trait 组合（不只是金/银/铜换色）

### Step 10：更新 frontend/

- index.html：title/Logo/footer 改 ATHENA
- style.css：注释改 ATHENA
- app.js：注释改 ATHENA

### Step 11：更新 requirements.txt

补齐新工具依赖：
- web3 (eas_attest)
- eth-account (eas_attest)
- chromadb (knowledge_base)
- sentence-transformers (knowledge_base)
- aiohttp (poc_generator)

### Step 12：更新 skills/glm-audit-skill/SKILL.md

- name 改为 athena-audit-skill
- description 改 Athena
- 内部引用改 Athena

### Step 13：检查合约中的 GLM 引用

- `contracts/AuditCertificate.sol`：SVG 底部 "GLM AUDIT" → "ATHENA"
- `contracts/AuditEvidenceChain.sol`：检查并修改
- `frontend/index.html` 底部 "Powered by GLM-5.1" → "Powered by GLM-5.1 | Athena"（保留模型名）

---

## 3. 验证清单

完成后检查：

- [ ] `grep -r "GLM Audit" . --include="*.md" --include="*.json" --include="*.html" --include="*.css" --include="*.js" --exclude-dir=.git --exclude-dir=lib` 返回 0 结果
- [ ] `grep -r "GLM AUDIT" . --include="*.md" --include="*.json" --include="*.html" --include="*.css" --include="*.js" --exclude-dir=.git --exclude-dir=lib` 返回 0 结果
- [ ] `grep -r "glm-audit" . --include="*.md" --include="*.json" --include="*.html" --include="*.css" --include="*.js" --exclude-dir=.git --exclude-dir=lib` 返回 0 结果（目录名除外）
- [ ] mcp/servers.json 包含 12 个工具
- [ ] expected-results.json 包含所有测试合约
- [ ] demo/nft-preview.html 标题为 ATHENA
- [ ] 所有 frontend 文件标题为 ATHENA

---

## 4. 保留 GLM-5.1 的地方

以下位置 **保留** GLM-5.1 作为模型名（它是 Z.AI 的模型，不是项目名）：

- "Powered by GLM-5.1"（前端 footer）
- 技术栈表格中 "GLM-5.1 作为驱动模型"
- GLM-5.1-TECHNICAL-PROFILE.md（整个文件保留）
- prompts 中 "powered by GLM-5.1"（作为模型描述）
- 合约 Metadata 中 "Auditor: GLM-5.1"（作为审计者名称）
