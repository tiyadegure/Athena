# Report Formatting

Save report to `{project-name}-audit-report-{timestamp}.md`.

## Output Format

Findings sorted by confidence (highest first). Each finding:
- Title with confidence score
- Contract.function location
- Description
- Fix (diff format)

Leads section for trails that couldn't be fully verified.
